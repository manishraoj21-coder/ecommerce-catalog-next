# E‑Commerce Product Catalog (Next.js)

- **Framework:** Next.js + React + TypeScript
- **API Routes:** `/api/products` returns products with simple filtering
- **Pages:** product grid, filters, cart (client state), mock checkout

## Run
```bash
npm install
npm run dev    # http://localhost:3000
```
